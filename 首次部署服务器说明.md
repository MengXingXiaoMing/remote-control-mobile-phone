# 首次部署到阿里云服务器

只需要做一次，以后服务器会 24 小时运行。整个部署都在电脑的管理面板里完成，不用登录服务器敲命令。

## 第一步：设置服务器密码

1. 登录阿里云控制台 → 轻量应用服务器 → 点你的服务器
2. 左侧「远程连接」→「重置密码」
3. 设置一个 root 密码（记好，后面部署要用）

## 第二步：开放 3000 端口

1. 阿里云控制台 → 服务器详情 → 防火墙
2. 添加规则：协议 TCP、端口 3000、允许（0.0.0.0/0）

服务器上如果还开着系统自带防火墙（firewalld/ufw），也要放行 3000 端口，否则 App 连不上。

## 第三步：在 config.txt 里填 IP

打开项目根目录的 `config.txt`（没有就复制 `config.txt.example` 改名为 `config.txt`），把 `你的服务器公网IP` 改成真实 IP：

```
SERVER_IP=47.123.45.67
```

## 第四步：生成 SSL 证书

中继服务器用自签名证书实现 WSS 加密。首次部署前，在**本地**生成一对证书（`server.crt` / `server.key`），放到 `relay-server/certs/` 目录——部署时管理面板会自动上传到服务器，无需手动登录服务器。

在 Git Bash（或 Linux/Mac 终端）里执行：

```bash
mkdir -p relay-server/certs
openssl req -x509 -newkey rsa:2048 -nodes \
  -keyout relay-server/certs/server.key \
  -out relay-server/certs/server.crt \
  -days 3650 \
  -subj "/CN=remote-control" \
  -addext "subjectAltName=IP:你的公网IP,DNS:localhost"
```

生成后，把 `server.crt` 的内容同步到安卓端（复制 `android-app/app/src/main/res/raw/server_cert.crt.example` 为 `server_cert.crt` 并替换内容），再构建 App。

## 第五步：部署服务器

1. 双击 `启动管理面板.bat`（或运行 `open_controller.ps1`），浏览器打开 `http://localhost:8899`
2. 页面中部「服务器配置（部署）」填写：服务器 IP、端口 22、用户名 root、root 密码、排队上限
3. 点「部署到服务器」，等待 1-3 分钟
4. 底部「输出」区显示部署成功即完成

部署会自动做三件事：上传服务器代码、安装依赖、启动服务（后台常驻，重启服务器也会自动拉起）。

## 第六步：验证是否成功

部署完成后，在同一管理页面顶部「远程控制」填服务器地址和配对码，能连上手机就说明成功。

也可以 SSH 登录服务器执行 `pm2 status`，看到 `relay` 状态为 online 即正常。

---

## 以后更新代码怎么办？

改了代码后，重新点管理面板里的「部署到服务器」即可，会自动上传并重启服务。

---

## 如果部署失败怎么办

**可能原因 1：防火墙没开 3000 端口**
- 阿里云控制台 → 服务器详情 → 防火墙，确认有 TCP 3000 允许规则
- 同时检查服务器系统防火墙是否放行 3000

**可能原因 2：IP 地址不对**
- 确认填的是公网 IP，不是内网 IP
- 公网 IP 可在阿里云服务器详情页看到

**可能原因 3：密码或 SSH 端口不对**
- root 密码输入错误，或服务器 SSH 端口不是默认的 22，请核对后重试
