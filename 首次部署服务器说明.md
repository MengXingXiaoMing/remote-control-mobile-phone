# 首次部署到阿里云服务器

只需要做一次，以后服务器会 24 小时运行。

## 第一步：设置服务器密码

1. 登录阿里云控制台 → 轻量应用服务器 → 点你的服务器
2. 左侧「远程连接」→「重置密码」
3. 设置一个 root 密码（记好，后面要用）

## 第二步：在 config.txt 里填 IP

打开项目里的 `config.txt`，把 `你的服务器公网IP` 改成真实的IP地址。

比如：
```
SERVER_IP=47.123.45.67
```

## 第三步：双击部署脚本

双击 `部署到服务器.bat`

会做三件事：
1. 上传服务器代码
2. 安装依赖
3. 启动服务（后台运行，关机也不怕）

过程中会让你输入 3 次 root 密码，输入就行（屏幕不显示是正常的）。

## 第四步：验证是否成功

部署完成后，双击 `打开控制页面.bat`，如果能打开网页说明成功了。

也可以在浏览器里直接访问：
```
http://你的公网IP:3000
```

能看到"远程控制"的登录页面就 OK 了。

---

## 以后更新代码怎么办？

如果你改了 server.js，重新双击 `部署到服务器.bat` 就行，会自动上传并重启服务。

---

## 如果部署失败怎么办

**可能原因 1：防火墙没开 3000 端口**
- 去阿里云控制台 → 服务器详情 → 防火墙
- 确认添加了 TCP 3000 端口的允许规则

**可能原因 2：IP 地址不对**
- 确认填的是公网IP，不是内网IP
- 公网IP可以在阿里云服务器详情页看到

**可能原因 3：密码输错**
- 重新输入密码试试
