# 平板/手机安装 App 说明

## 方法一：用 Android Studio 构建（推荐，最靠谱）

### 第一步：安装 Android Studio

1. 下载：https://developer.android.com/studio
2. 安装时一路下一步，默认选项就行
3. 第一次打开会下载 SDK，等它下完

### 第二步：打开项目

1. 打开 Android Studio
2. 点 **Open an existing project**
3. 选择文件夹：`e:\code\ai solo\远程控制手机\android-app`
4. 等待右下角 Gradle 同步完成（进度条走完）

### 第三步：构建 APK

1. 顶部菜单 → **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. 等待构建完成（下方会有进度条）
3. 构建成功后会弹出提示，点 **locate** 就能找到 APK 文件

APK 文件位置：
```
android-app\app\build\outputs\apk\debug\app-debug.apk
```

### 第四步：安装到平板

**方式 A：USB 连接安装**
1. 平板用数据线连电脑
2. 平板上开启「开发者选项」→ 打开「USB调试」
3. Android Studio 顶部选好你的平板设备
4. 点 ▶ Run 按钮，自动安装

**方式 B：发文件过去安装**
1. 把 `app-debug.apk` 发到平板上（微信/QQ/邮箱都行）
2. 平板上点开这个 APK 文件
3. 提示"未知来源" → 点允许 → 安装

---

## 方法二：如果不想装 Android Studio

可以让我帮你用命令行构建，但需要装 JDK 和 Android SDK，比较麻烦。还是推荐装 Android Studio，以后改代码也方便。

---

## 首次打开 App 需要做的设置

安装后第一次打开，按下面步骤配置：

### 1. 开启无障碍服务

1. 打开「远程控制」App
2. 点「开启无障碍服务」按钮
3. 系统设置里找到「远程控制」
4. 打开开关 → 确认开启

### 2. 填写服务器地址

在 App 的「服务器地址」栏填：
```
ws://你的阿里云公网IP:3000
```

比如：`ws://47.123.45.67:3000`

填好后 App 会自动保存，下次打开不用再填。

---

## 每次使用流程

### 妈妈的平板上：
1. 打开「远程控制」App
2. 点「开始远程控制」
3. 弹框点「立即开始」
4. 把屏幕上的 6 位配对码告诉你

### 你的电脑上：
1. 双击 `打开控制页面.bat`（在 `e:\code\ai solo\远程控制手机\` 里）
2. 输入配对码 → 点连接
3. 等妈妈在平板上点「允许」
4. 开始远程操作

---

## 常见问题

**Q: 安装时提示"风险应用"怎么办？**
A: 因为是自己打包的 APK，没有签名，系统会提示。点「仍要安装」或「允许本次安装」就行。

**Q: 平板上找不到"无障碍服务"在哪？**
A: 每个品牌位置不一样：
- 华为/荣耀：设置 → 智慧助手 → 无障碍
- 小米/红米：设置 → 更多设置 → 无障碍
- OPPO/vivo：设置 → 其他设置 → 无障碍
直接在设置里搜"无障碍"最快。

**Q: 服务器地址填什么？**
A: 填你阿里云服务器的公网IP，前缀是 `ws://`，后缀是 `:3000`。
比如你的IP是 47.123.45.67，就填 `ws://47.123.45.67:3000`
